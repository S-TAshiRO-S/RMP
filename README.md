📘 Лабораторная работа №1: Знакомство с Flutter. Создание базового UI
🎯 Цель работы

Освоить начальную настройку среды разработки для Flutter (VS Code или Github Codespaces).

Познакомиться с процессом создания, запуска и публикации Flutter-проекта.

Изучить базовые виджеты Flutter:

Container

Row

Column

Text

Научиться создавать простой пользовательский интерфейс с использованием Scaffold и интерактивных элементов (FloatingActionButton).

🧩 Содержание проекта

Проект представляет собой простое Flutter-приложение, демонстрирующее работу с базовыми и интерактивными виджетами.

В интерфейсе используются:

Scaffold — базовая структура экрана.

Column и Row — для компоновки элементов.

Container — для создания разноцветных блоков.

Text — для отображения текста.

FloatingActionButton — интерактивная кнопка, при нажатии на которую в консоль выводится сообщение.

CircleAvatar — круглые аватары, один из которых загружает изображение по сети через NetworkImage.

🖼️ Пример интерфейса

Интерфейс состоит из:

Верхнего синего контейнера с текстом.

Ряда из трёх равномерно распределённых текстовых элементов.

Expanded с двумя CircleAvatar (один локальный цветной, другой — с изображением из интернета).

Нижнего зелёного контейнера.

Плавающей кнопки (FloatingActionButton) в правом нижнем углу.

▶️ Как запустить проект

Установите Flutter SDK

Клонируйте репозиторий:
git clone https://github.com/S-TAshiRO-S/RMP.git
cd RMP/flutter_application
Установите зависимости:
flutter pub get
Запустите проект (на эмуляторе, устройстве или в браузере):
flutter run
При нажатии на кнопку + в консоль выведется сообщение:
Button pressed!


📘 Лабораторная работа №2: Подключение Supabase. Экран сообщений и работа с базой данных
🎯 Цель работы

Научиться подключать Flutter-приложение к Supabase (PostgreSQL) через пакет supabase_flutter.

Создать таблицу в Supabase и организовать:

чтение списка сообщений из базы,

добавление сообщения (insert) из приложения.

Отработать работу с асинхронностью во Flutter:

FutureBuilder

ListView / List.generate

получение данных из сети (REST через Supabase SDK)

🧩 Содержание проекта

Проект представляет собой простое приложение “Сообщения”:

При запуске приложение загружает список сообщений из таблицы public.messages в Supabase и отображает их списком.

Внизу экрана есть поле ввода и кнопка Отправить:

текст отправляется в базу (INSERT),

после отправки список обновляется (сообщение появляется сверху/в списке).

🗄️ База данных (Supabase)

Используется таблица:

public.messages

Пример структуры:

id — автоинкремент (primary key)

content — текст сообщения

created_at — время создания (timestamp)

📁 Где находится проект

Лаба №2 расположена в репозитории в папке:

2lab/supa_chat_lab

⚙️ Конфигурация Supabase

Для подключения используются данные проекта Supabase:

supabaseUrl

supabaseAnonKey

Файл конфигурации лежит в:
2lab/supa_chat_lab/assets/app_config.json

⚠️ Репозиторий публичный, но ключ используется publishable/anon, поэтому он подходит для демонстрации и проверки.

✅ Результат

В Supabase добавляются записи (в том числе на русском), а приложение корректно:

отображает список сообщений,

добавляет новые сообщения в таблицу,

показывает дату/время сообщения.


📘 Лабораторная работа №3: Авторизация и регистрация через Supabase

🎯 Цель работы

Реализовать регистрацию и авторизацию пользователей через Supabase.

Использовать BottomNavigationBar для переключения экранов.

Сохранять username в SharedPreferences для автологина.

🧩 Функционал

BottomNavigationBar (2 пункта):

Авторизация

Регистрация

Экран авторизации

Поля: логин, пароль

При вводе данных выполняется SELECT в таблице users

При успешном входе:

сохраняется username в SharedPreferences

переход на экран сообщений

Экран регистрации

Поля: логин, пароль

При вводе данных выполняется INSERT в таблицу users

После успешной регистрации — переход на экран авторизации

Автовход

При старте приложения проверяется сохранённый username

Если он есть — сразу открывается экран сообщений

Выход

Удаление username из SharedPreferences

Возврат на экран с BottomNavigationBar

🗃️ Структура экранов

MainScreen — BottomNavigationBar

AuthScreen — авторизация

RegistrationScreen — регистрация

MessagesScreen — экран сообщений

✅ Как проверить, что лаба сделана

На Регистрации введи логин/пароль → зарегистрируйся → запись появляется в таблице users.

На Авторизации введи логин/пароль → вход → открывается экран сообщений.

Перезапусти приложение → если username сохранён → сразу открываются сообщения.

Нажми Выйти → SharedPreferences очищается → возврат на главное меню.
